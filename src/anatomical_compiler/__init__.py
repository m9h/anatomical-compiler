"""anatomical-compiler: shared library code (training, control, analysis).

The historical home of the project's logic is the ``scripts/`` directory;
this package collects reusable building blocks so benchmarks and tests can
import them instead of duplicating code.
"""

__version__ = "0.1.0"
